# Server
PORT=4000
PUBLIC_BASE_URL=http://localhost:5173

# Database (choose ONE)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=termine
# or use DATABASE_URL=postgres://user:pass@host:5432/db
# or local dev fallback (no Docker needed):
# SQLITE_FILE=./dev.sqlite

# Auth
JWT_SECRET=please_change_me

# Email (SendGrid)
SENDGRID_API_KEY=
MAIL_FROM="Online-Termine <noreply@meinetermine.de>"

# SMS (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=